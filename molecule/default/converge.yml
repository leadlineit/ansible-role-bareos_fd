---
- name: Converge
  hosts: all
  tasks:
    - name: Install requirements
      apt:
        name: gnupg
        state: present
        update_cache: true
    - name: "Include leadlineit.bareos_fd"
      include_role:
        name: "leadlineit.bareos_fd"
